# OrbitSmith Merge Notes

This kit is a **true unified workspace**:
- Core agent scaffold sourced from `vudovn/antigravity-kit` (`kit/.agent`)
- Skill + docs library sourced from `sickn33/antigravity-awesome-skills` (`kit/skills`, `kit/docs`)

## Naming convention
- Keep upstream names when unique.
- If collisions appear, apply prefixes: `kit-` (framework) and `aws-` (awesome-skills).

## Next curation pass
1. Resolve skill filename collisions
2. Standardize front-matter schema
3. Add one canonical `AGENTS.md` and `/commands` map
